<h2>プロフィール編集</h2>
<%= form_with model: @book do |f| %>
  <label for="title">Title</label>
  <%= f.text_field :title, placeholder: "タイトル" %>
  <label for="caption">caption</label>
  <%= f.text_field :caption, placeholder: "内容" %>  
  <%= f.submit "変更を保存" %>
<% end %>